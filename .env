# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=intelligent_id_db

# Server Configuration
PORT=3000
NODE_ENV=development

# Session Configuration
SESSION_SECRET=your_secret_key_here_change_this

# API Configuration
API_BASE_URL=http://localhost/thesis

# Twilio SMS provider (optional) - set these to enable real SMS sending
# TWILIO_SID and TWILIO_TOKEN are required for Twilio REST API authentication
# TWILIO_FROM is the phone number in your Twilio account (E.164 format) used as the sender
TWILIO_SID=
TWILIO_TOKEN=
TWILIO_FROM=
QR_SECRET=
